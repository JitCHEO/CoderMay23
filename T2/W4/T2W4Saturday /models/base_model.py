# from main import db


# #  put the common thing in here that can be used in other MODEL
# class BaseModel(db.Model):
#     id = db.Column(db.Integer, primary_key=True)
#     created_at = db.Column(db.DateTime)
